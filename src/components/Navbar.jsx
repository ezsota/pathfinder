// logo + standard navigation + public site navigation + login
import { Link } from "react-router-dom";

export default function Navbar() {
    return (
        <p>This is the public navbar</p>
    );
};