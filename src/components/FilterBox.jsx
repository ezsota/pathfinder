//filter box (make this a rectangle with an image in the background) -> filter list of displayed crafts

export default function FilterBox() {
    return (
        <p>This is where the filter box goes</p>
    );
};